# 模型评判可以按这次改了哪些文件打分

<!-- release-target: v2 -->

## 新增功能

- 评分 Prompt 新增 `{{files}}` 占位符。模型评判此前只能读到最终那段回答，现在可以把这次运行新建、修改和删除的文件清单一起交给评判模型；清单一次最多列出 200 个路径，超出时会写明还有多少个没列出。
- 新增内置评判维度「声称与实际改动一致」，比对回答里声称改过的文件和实际改动的文件，抓的是「说改了其实没改」和「改了却没提」这两类偏差。
- 轨迹里读不到文件路径时，这个维度记为未评分并写明缺少证据，不会被当成「什么都没改」判成不通过。
