# 评测可以给一次工作的开销设预算

<!-- release-target: v2 -->

## 新增功能

- 评测新增「轨迹预算」评分器：可以给一次运行设定轮数、工具调用次数、token 与耗时上限，用来判断这次工作有没有绕远路、烧掉不该烧的开销。不超支得满分，超支按超出程度扣分而不是直接判不合格，所以「超一点」和「超十倍」分得开；留空的指标不设预算。
- 这项评分只对有轨迹的来源生效：Runtime 没有上报的指标会被跳过而不是算成超支，直接评一个文件夹这类没有轨迹的来源会明确显示为已跳过，不会打 0 分。
