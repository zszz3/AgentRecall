# MCP 引用编辑与 JSON 校验体验修复

## Bug 修复

- MCP Server 详情页里编辑环境变量 / 请求头的名称时，输入框不再每敲一个字符就失去焦点、条目也不再跳到列表末尾，可以正常连续输入；把名称改成已存在的条目时会显示重复提示，不再静默合并覆盖。
- 以 JSON 编辑 Server 时，`disabledTools` 里填了尚未发现的工具名（或 Server 还没做过连接测试）会立即得到明确报错，不再出现"保存后配置悄悄消失"的情况。
