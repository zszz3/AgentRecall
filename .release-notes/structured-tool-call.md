# Codex Code Mode 工具记录更准确

<!-- release-target: both -->

## 新增功能

- Codex Code Mode 会话现在会将真实执行结果归入 `exec` 父调用，明确标注从代码中静态解析出的工具，并在每个结果卡片后展示对应的解析信息；Skill 使用统计也更准确，仅在代码中出现、执行失败或被拒绝的调用不会误计为成功使用。

## Bug 修复

- 修复 Codex Code Mode 会话增量更新后，同一次内层工具调用可能同时显示静态记录与运行时记录的问题。
