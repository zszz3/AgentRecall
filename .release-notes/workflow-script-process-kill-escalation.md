# 修复 Workflow 脚本停不下来时残留后台进程

<!-- release-target: v2 -->

## Bug 修复

- 修复 Workflow 脚本节点超时或被取消时，只发一次正常停止信号就立刻报告「已停止」的问题：脚本若不响应这个信号，就会一直在后台运行、占着工作目录和端口，而应用已经交出了控制权，再也停不掉它。现在会在短暂宽限后强制结束，并确认进程真正退出后才报告超时或已取消；外部取消与中止信号走同一条停止路径，正常响应停止信号的脚本仍然立即结束，不会多等。
