# 评测运行会显示同一用例多次重复之间的波动

<!-- release-target: v2 -->

## 新增功能

- 评测运行结果新增「波动」指标：同一个用例重复跑多次时，报告它最好一次与最差一次的分差，以及有多少个用例跑了不止一次。两个方案平均分相同、波动却差很多时，波动大的那个连自己的分数都复现不出来，这一点过去被平均值掩盖了。
- 只跑过一次的用例不显示波动，也不会被当成「很稳定」；因为评判器自身原因没能评分的那一次会被排除在波动之外，而不是按 0 分计入，避免把工具故障算成被测对象不稳定。
