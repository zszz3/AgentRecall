# 长期记忆恢复可用

<!-- release-target: v2 -->

## Bug 修复

- 修复长期记忆无法检索的问题：此前写入的记忆不会进入检索索引，导致对话中始终召回不到任何记忆。现在历史记忆可以正常搜索与召回。
- 应用升级后如果长期记忆运行时版本不匹配，现在会自动安装并启动，不再出现记忆功能悄悄停用的情况。
- 缺少本地向量模型时会自动下载后再启动记忆服务。
- 记忆运行时安装或启动失败时会给出明确的失败阶段说明，便于排查。
