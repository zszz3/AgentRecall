# OpenViking 运行监控

<!-- release-target: v2 -->

## 新增功能

- 记忆页面新增 OpenViking 运行监控，可查看服务健康、进程信息与导入任务状态，并直接启动、关闭或重启服务。

## Bug 修复

- 暂停、停止管理或删除一个目录时，不再导致其他目录的记忆读取与导入中断。
- 目录清理失败后，其他目录不再错误地保持暂停状态。
- 修复多个目录同时恢复记忆凭据时可能丢失凭据的问题。
- 手动记忆的标题在保存和刷新后会保持不变。
- 取消首次导入后，记忆页面不再持续刷新。
- 重启后不再把无关进程误判为正在运行的记忆服务。
