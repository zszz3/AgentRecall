# Eval：查看 Skill 触发所在的会话

<!-- release-target: v2 -->

## 新增功能

- ⚗️ 新增 Eval 能力（默认关闭）：在设置中开启并安装使用统计 Hook 后，Eval 页可按 Skill 查看最近的触发记录，并直接打开每次触发所在的会话，回看这个 Skill 当时到底做了什么。
- Codex 会话里的 Skill 触发无需任何配置即可自动关联；早期没有关联信息的触发记录会如实标注为"未关联"，不做猜测归因。
