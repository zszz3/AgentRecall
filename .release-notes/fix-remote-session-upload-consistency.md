# 修复完整会话的云端同步

<!-- release-target: both -->

## Bug 修复

- 大型 Cursor 会话现在会完整分块上传，不再因单个文件超过云端大小限制而失败。
- 完整会话的分块和附件现在会并行上传，减少大型会话保存到云端时的等待。
- 当可见消息数相同但完整 Cursor 会话仍有变化时，云端同步状态会明确提示完整会话需要更新。
- Codex 和 Claude 会话现在只展示当前有效对话，同时云端仍会保留完整原始会话，跨 Agent 恢复不再混入已撤销内容。
- 已失去远端源文件的 SSH 会话缓存现在可以直接删除。
